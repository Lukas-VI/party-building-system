P1 桌面端设置角色无法撤回, 在编辑人员信息后，可能会被赋予多重角色，无法被撤回，导致权限与视图错误

P1 上传文件无法删除撤销

P0 手机端上传页面，文件上传后无法预览，空白

P0 审核界面，预览同样失效，且会触发下载。

P3 桌面端右侧应该放到独立的滚动容器里，保持左侧菜单固定

P0 申请书、等文件，在节点里，要求填写的提交时间，应该精确到日即可。

P2 手机端使用输入法，底部bar会随输入法一起上移

P3 页面切换、资源加载、列表、折叠的展开需要过渡动画

P1 每个页面需要有返回按钮，在top bar，在左上角，是个 <返回

P1 移动端，消息需要有视觉提醒，在底部bar的图标上加个红点提醒

P2 申请人/审核者 申请改期功能需要折叠起来

P0 谈话状态错误。作为通知类节点，应该先由审核者填写好地点时间后下发任务，发消息通知，申请人才能看到进行中，否则显示未开放。

P0 所有通知类节点，需要审核人主动填写后发送消息。对于申请人，在收到消息之前都是进行中。

P2 审核节点详情页面，申请人的名字等信息太小了看不见

P3 申请人，改期需要时间选择菜单

P3 待办事项顺序需要优化

P1 快捷入口-待审核节点，数量异常，应该是后端实时算的。

P0 没有 流程资格确认 删掉

P1 通知无法实时刷新

P1 任务卡片，审核看到的申请人名字太小了

P1 申请人 暂时除了未通过的节点可以提交更改申请，别的都不能再次提交

P0 在提交类节点中，在申请人还没有提交时处于进行中状态，审核人就已经能在待办中看到。应该提交后待审核才变成审核的待办，能看到。对于通知类，开始是审核的待办。

P2 类似  `blessingText: isCompleted ? ${step.name}已完成，请继续关注后续流程通知。` : '',全删了，保持节点详情页面整洁

P0 对于通知类：同意、不同意——确认，提交类：通过、不通过——确认，这样决定审核结果。

P0 完成所有节点后，流程办理-当前节点，又会显示第一个节点。需要显示已全部完成，隐藏当前节点
