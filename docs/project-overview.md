# 项目总览

## 1. 项目定位
本项目用于支撑党务管理和发展党员流程的数字化管理，当前阶段以“可部署、可联调、可继续扩展”为目标。手机端正式方向已调整为“微信服务号网页 App + PC 后台”的双端形态。

## 2. 当前项目结构
- `server/`：Node.js 服务端，负责认证、权限、流程、统计、导出、上传
  - `server/src/app.js`：Express 应用组装与路由注册
  - `server/src/app-context.js`：跨路由共享的认证、权限、流程、上传和序列化规则
  - `server/src/routes/`：按业务域拆分的 HTTP 路由
- `admin-desktop/`：PC 后台管理端
- `admin-mobile/`：服务号网页 App（Vue3 + Vant）
- `docs/`：项目文档、图表、部署说明

## 3. 当前进展
- 已完成 PC 后台管理端基础搭建
- 已完成服务端从 SQLite 演示版向 MySQL 部署版迁移
- 已完成 JWT 鉴权和统一响应格式
- 已完成部署文件、环境变量模板和反向代理配置模板
- 已接入 Ubuntu 开发服务器作为下一阶段联调环境
- 已新增微信账号绑定的数据模型和接口骨架
- 已具备后台、服务号网页 App 与服务端的真实联调条件
- 已新增 `user_profiles`，用于存储基层管理角色和管理员资料
- 已补充管理员账号修复脚本与 `1919` 统一前端网关联调口径
- 已决定将桌面后台与服务号网页 App 分开构建，避免继续在桌面后台上硬做手机适配
- 已新增 `admin-mobile/`，用于承接服务号网页 App
- 已将桌面后台与服务号网页 App 分别收口到 `/admin-desktop/` 与 `/wx-app/`
- 已新增《发展党员全程记实表》整理文档，作为 `25 步` 填写/提交内容的研发参考
- 历史小程序入口、页面和依赖已从主仓库路径清理，移动端只保留服务号 H5 主线
- 前 12 步已作为第一阶段 MVP 办理边界，后续步骤暂不进入待办和办理接口
- 已新增 Ubuntu 发布脚本和统一前端网关/frpc 启动后检查
- 服务端正在模块化为 `app.js`、`app-context.js` 与 `routes/*`，降低单文件维护压力

## 4. 需求分析
核心需求包括：
- 账号登录与首次注册
- 服务号网页授权与账号绑定
- 发展党员 25 步流程管理
- 分角色、分层级的数据权限
- PC 端台账查看、统计分析和数据下载
- 服务号网页 App 工作台、资料维护、材料上传、消息提醒与流程办理
- 发展党员全程记实表结构整理与后续字段映射
- 附件上传、流程留痕、操作审计

## 5. 核心模块说明
- 认证模块：账号密码登录、JWT、角色菜单
- 微信模块：服务号网页授权、账号绑定状态与解绑
- 全程记实表参考模块：整理纸质表结构，为后续 `25 步 -> 字段/材料` 映射提供依据
- 权限模块：本人/支部/单位/全校数据范围
- 流程模块：步骤定义、步骤记录、审核提交
- 资料模块：申请人资料与管理角色资料分开建模
- 统计模块：单位统计、支部统计、阶段分布
- 导出模块：申请人台账、流程台账、统计报表
- 上传模块：附件持久化与公网 URL 返回

## 6. 数据模型
- 详细 ER 图见 [er.mmd](er.mmd)
- 数据流见 [dfd.mmd](dfd.mmd)
- 全程记实表整理见 [electronic-dossier.md](electronic-dossier.md)

主要实体：
- `users`
- `roles`
- `permissions`
- `user_roles`
- `role_permissions`
- `org_units`
- `branches`
- `registration_requests`
- `applicant_profiles`
- `user_profiles`
- `workflow_instances`
- `workflow_step_definitions`
- `workflow_step_records`
- `attachments`
- `audit_logs`
- `wechat_bindings`
- `notifications`
- `notification_receipts`

## 7. 当前风险与待办
- 服务号网页 App 仍需公网 `/wx-app/` 反代路径正式接通
- MySQL 生产库初始化需单独执行
- 生产环境建议补充更强密码加密和审计策略
- 后续应补充数据库迁移工具，而不是仅依赖初始化脚本
- 服务号网页授权仍缺少真实 `AppID/AppSecret/Redirect URI` 才能联调
- 《发展党员全程记实表》目前仅整理为研发参考文档，后续仍需结合会议纪要补齐 25 步字段映射
- P1 安全项仍需继续处理：强密码哈希、生产密钥强制校验、注册阶段密码暂存与审批后激活
- 正式验收前仍需处理：注册姓名/身份证/学工号一致性校验、PC 注册审核页面、服务号模板消息、政审分项审核、时间轴、真实 Excel 导入
